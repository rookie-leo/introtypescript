// Arrays
let numeros: number[] = [3, 4, 5];
let numeros2: Array<number> = [7, 8, 9];

// Tuple
let x:  [string, number];
x = ["Noel", 65];

// Enum
enum Cor{Red, Green, Blue};
let corFundo: Cor = Cor.Blue;

console.log(numeros[0]);